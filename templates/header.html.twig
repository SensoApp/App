{% extends 'base_old.html.twig' %}
{% block body %}
    <div class="container-fluid">
        <!-- header-area start -->
                <div class="row">
                    <!-- Navigation -->
                        <nav class="navbar navbar-default navbar-light">
                            <!-- Brand and toggle get grouped for better mobile display -->
                            <div class="navbar-header">
                                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".bs-example-navbar-collapse-1" aria-expanded="false">
                                    <span class="sr-only">Toggle navigation</span>
                                    <span class="icon-bar"></span>
                                    <span class="icon-bar"></span>
                                    <span class="icon-bar"></span>
                                </button>
                                <a href="http://www.senso.lu/" >
                                    <img src="{{ asset('img/Logo_Senso_3.png') }}" alt="logo">
                                </a>
                                <!-- Brand -->
                            </div>
                            <!-- Collect the nav links, forms, and other content for toggling -->
                            <div class="collapse navbar-collapse main-menu bs-example-navbar-collapse-1" id="navbar-example">
                                <ul class="nav navbar-nav navbar-right" >
                                    <li class="active">
                                    <li>
                                        <a class="page-scroll" href="{{ path('simulation') }}">Simulation</a>
                                    </li>
                                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                        <li>
                                            <a class="page-scroll" href="{{ path('app_logout') }}">Logout</a>
                                        </li>
                                        <li>
                                            <div class="dropdown">
                                                <button class="btn btn-primary btn-md dropdown-toggle" type="button" data-toggle="dropdown">
                                                    Menu
                                                    <span class="caret"></span>
                                                </button>
                                                <ul class="dropdown-menu" style="background-color: whitesmoke !important; max-height: 400px; overflow: auto" id="topNav">
                                                    {% if is_granted('ROLE_ADMIN') %}
                                                        <li class="dropdown-header text-center">Admin section</li>
                                                        <li><a class="btn btn-info" href="{{ path('adminsenso') }}">Admin</a></li>
                                                        <li class="dropdown-header text-center">Client creation</li>
                                                        <li><a class="btn btn-info"  href="{{ path('client_contract') }}">Create new Client Contract</a></li>
                                                        <li><a class="btn btn-info" href="{{ path('create_client') }}">Create new Client</a></li>
                                                        <li class="dropdown-header text-center">User & Contact creation</li>
                                                        <li><a class="btn btn-info" href="{{ path('createcontact') }}">Create new contact</a>  </li>
                                                        <li><a class="btn btn-info" href="{{ path('app_register') }}">Create new user</a></li>
                                                        <li class="dropdown-header text-center">Invoices & Statements</li>
                                                        <li><a class="btn btn-info"  href="{{ path('listofinvoice') }}">List of Invoices</a></li>
                                                        <li><a  class="btn btn-info" href="{{ path('saveManualInvoice') }}">Create Invoices</a></li>
                                                        <li><a  class="btn btn-info" href="{{ path('randomInvoice') }}">Create Random Invoices</a></li>
{#                                                        <li><a  class="btn btn-info" href="{{ path('uploadstatement') }}">Statements</a></li>#}
                                                        <li><a  class="btn btn-info" href="{{ path('statementAdmin') }}">Statements Summary</a></li>
                                                        <li><a  class="btn btn-info" href="{{ path('statementEntry') }}">Statements Manual Entry</a></li>
                                                        <li class="dropdown-header text-center">User section</li>
                                                        <!-- <li class="men btn "><a class="men" href="{{ path('timesheet') }}" >Create new Timesheet</a></li> -->
                                                        <li><a  class="btn btn-info" href="{{ path('user_dashboard') }}">Dashboard</a></li>
                                                    {% else %}
                                                        <li class="dropdown-header text-center">User section</li>
                                                        <!--  <li class="men btn "><a class="men" href="{{ path('timesheet') }}" hidden>Create new Timesheet</a></li> -->
                                                        <li><a class="btn btn-info" href="{{ path('user_dashboard') }}">Dashboard</a></li>
                                                    {% endif %}
                                                </ul>
                                            </div>
                                        </li>
                                    {% else %}
                                        <li>
                                            <a class="btn btn-success"  href="{{ path('adminsenso') }}"><i>My</i>Senso</a>
                                        </li>
                                    {% endif %}
                                </ul>
                            </div>
                            <!-- navbar-collapse -->
                        </nav>
                        <!-- END: Navigation -->
                </div>
        <!-- header-area end -->
    </div>
{% endblock %}